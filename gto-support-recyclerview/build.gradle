android {
    defaultConfig {
        consumerProguardFiles 'proguard-consumer.pro'
    }
}

dependencies {
    api "androidx.recyclerview:recyclerview:${deps.androidX.recyclerView}"
    implementation('com.github.Karumi:WeakDelegate:1.0.0') {
        exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
    }

    // Data Binding adapter dependencies
    compileOnly "androidx.databinding:databinding-runtime:${deps.gradleAndroidPlugin}"
}

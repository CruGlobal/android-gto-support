android {
    defaultConfig {
        consumerProguardFiles 'proguard-consumer.pro'
    }
}

dependencies {
    api "androidx.recyclerview:recyclerview:${deps.androidXRecyclerView}"

    compileOnly "androidx.databinding:databinding-runtime:${deps.androidXDataBinding}"
}

android {
    defaultConfig {
        consumerProguardFiles 'proguard-consumer.pro'
    }
}

dependencies {
    api "com.android.support:recyclerview-v7:${deps.androidSupport}"

    compileOnly "com.android.databinding:library:${deps.androidDataBinding}"
}

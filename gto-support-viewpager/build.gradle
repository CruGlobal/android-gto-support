dependencies {
    implementation project(':gto-support-util')

    api "com.android.support:support-core-ui:${deps.androidSupport}"
    compileOnly "com.android.support:support-fragment:${deps.androidSupport}"

    implementation "com.jakewharton.timber:timber:${deps.timber}"
}

language: android
sudo: false
jdk:
  - oraclejdk8

android:
  components:
    - platform-tools
    - tools
    - build-tools-23.0.1
    - android-23
    - extra-android-m2repository

cache:
  directories:
    - $HOME/.gradle/caches/2.12
    - $HOME/.gradle/caches/jars-1
    - $HOME/.gradle/daemon
    - $HOME/.gradle/native
    - $HOME/.gradle/wrapper

script:
  - ./gradlew build -x lint

notifications:
  email: false

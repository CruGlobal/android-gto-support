dependencies {
    api "com.squareup.picasso:picasso:${deps.picasso}"

    implementation "com.nhaarman.mockitokotlin2:mockito-kotlin:${deps.mockitoKotlin}"
    compileOnly "junit:junit:${deps.junit}"
}

apply plugin: 'kotlin-parcelize'

dependencies {
    api "androidx.collection:collection:${deps.androidX.collection}"

    testImplementation "androidx.collection:collection-ktx:${deps.androidX.collection}"
}

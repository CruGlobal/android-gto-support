android {
    lintOptions {
        disable 'InvalidPackage'
    }
}

dependencies {
    implementation 'org.mockito:mockito-core:1.10.19'
    implementation "org.powermock:powermock-api-mockito:${deps.powermock}"
    implementation "com.google.guava:guava:${deps.guava}"
}

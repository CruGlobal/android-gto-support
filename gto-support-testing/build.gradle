android {
    lintOptions {
        disable 'InvalidPackage'
    }
}

dependencies {
    implementation "org.mockito:mockito-core:${deps.mockito}"
    implementation "org.powermock:powermock-api-mockito2:${deps.powermock}"
    implementation "com.google.guava:guava:${deps.guava}"
}

android {
    defaultConfig {
        consumerProguardFiles 'proguard-consumer.pro'
    }
}

dependencies {
    implementation project(':gto-support-base')
    implementation project(':gto-support-compat')

    implementation "com.squareup.picasso:picasso:${deps.picasso}"

    compileOnly "com.android.support:appcompat-v7:${deps.androidSupport}"
    compileOnly "com.crashlytics.sdk.android:crashlytics:${deps.crashlytics}"
}

android {
    defaultConfig {
        consumerProguardFiles 'proguard-consumer.pro'
    }
}

dependencies {
    implementation project(':gto-support-okhttp3')

    implementation "com.jakewharton.timber:timber:${deps.timber}"
    implementation "com.snowplowanalytics:snowplow-android-tracker:${deps.snowplow}"
}
